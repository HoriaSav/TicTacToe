- css styling
- junit tests
- settings implementation
- use dependency injection instead of direct instantiation


Problems (code):
- draw should not result in a win

Problems (UI):
- "-" button should appear only for non-guests
- window should be resizable (inside settings)

optional:
- leaderboard
- delete users
