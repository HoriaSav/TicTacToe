- css styling
- junit tests
- settings implementation
- implement interfaces for better management of code (game and player service)
- use dependency injection instead of direct instantiation


Problems:
- can enter 2 users with same name
- create field should empty after pressing create button
- "-" button should apear only for non-guests
- game does not work for guests
- window should be resizable
- window should be movable
- can create user with empty name
- can win games if in a line there is XX0 or X0X any wrong combination (only for same name users)

optional:
- leaderboard
